/** @type {string} localStorage key for guest-mode sleep data. */
export const GUEST_DATA_KEY = 'sync_guest_data';

/**
 * localStorage key for offline backup when the API is unreachable.
 * @deprecated Target removal: 2026-06 when backend availability is guaranteed.
 * @type {string}
 */
export const OFFLINE_BACKUP_KEY = 'circadian-calculator-data';

/** @type {string} localStorage key tracking onboarding completion. */
export const ONBOARDING_KEY = 'sync-onboarding-complete';
